{
  "name": "mcp-service",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "generate": "prisma generate --schema=../api/prisma/schema.prisma",
    "dev": "npm run generate && node --loader ts-node/esm --watch src/index.ts",
    "build": "npm run generate && tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.3.6",

    "@prisma/client": "^6.3.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.3.0",
    "prisma": "^6.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
